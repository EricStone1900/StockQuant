# 阶段一至六执行收口记录

日期：2026-09-23（Asia/Shanghai）

## 已完成

- 观察汇总增加最终日终快照语义；普通健康巡检不会覆盖已经确认的日终结果，实际缺口或失败仍会使该日回到 `WAITING`。新增 8 个汇总测试。
- V3.1 Runner 资源、网络策略、实验标识和受管挂载路径校验已生效；增加有界执行器，具备进程组硬超时和输出配额。
- PostgreSQL 预算账本已接入研究服务实验创建、预算查询和结算接口；重复预留、UNKNOWN 重试、结算修改和并发超额均受保护。
- BaoStock 独立恢复探测已接入巡检；探测失败返回 `DEGRADED` 和退出码 2，并保留尝试证据。2026-09-23 实际探测结果为 BaoStock `TIMEOUT`，Sina 继续可用。
- 自动化校验覆盖晨间、巡检、日终、一次性切换和旧任务的状态、周期及到期规则。
- NATS JetStream 和 Temporal 本地持久卷已按阶段五记录生效；当前容器仍健康运行。

## 验证结果

- 研究服务单测：24/24 PASS。
- 全仓 lint、typecheck、test：PASS；其中市场数据服务单测 35/35、平台 API 28/28。
- Python 适配器：44/44 PASS。
- 运维脚本：80/80 PASS。
- 观察汇总：8/8 PASS；观察采集：4/4 PASS；来源降级分类：2/2 PASS。
- 文档链接：444/444；契约 JSON：24；Fixture hash：10，均通过。
- 当前 V2.4 Paper 最终观察仍为 7/20；DC-08A 归档口径为 4/20，均保持 `WAITING`。

## 尚未闭合

- 市场数据服务新的调度 `mode` 状态字段已经通过 TypeScript 检查，但 Docker 镜像构建因容器内 npm registry 下载最后一个依赖长期无响应而中止，运行中的旧镜像仍返回 `FIXTURE_PLAN_ONLY`。未删除卷、未重启旧服务。
- V3.1 真实模型网关、受控 Runner 分发、Provider 出站限制和真实 RD-Agent 调用仍保持 `NOT_CONFIGURED` / `NOT_RUN`。
- BaoStock 仍为 `OPEN`，最近恢复探测为 `TIMEOUT`；Sina 是当前实际可用来源。
- 真实交易日观察、Ubuntu 实机和异机灾备仍须按实际发生分别验收。

本记录不改变既有人工签署，也不把代码测试或一次探测当作真实来源恢复或 V3.1 通过证据。
