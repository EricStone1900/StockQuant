# P3.6 V2.1/V2.2 TestRun 持久化

日期：2026-09-25（Asia/Shanghai）。

## 改动

- V2.1 和 V2.2 acceptance controller 移除进程内 `Map`，创建结果写入既有 `acceptance_stage_runs`。
- 创建和查询入口统一使用本地 acceptance owner；查询通过 `(testRunId, ownerId)` 读取，跨 owner 返回 403/不可见。
- V2.1 保留来源、采样和新闻断言；V2.2 保留分钟导入、质量拒绝和 SHA 幂等断言；持久化证据明确 `environmentMode`、`dataMode` 和 `brokerMode`。

## 验证

- `pnpm --filter @stockquant/platform-api-service test`：10 个测试文件、35/35 通过。
- `pnpm --filter @stockquant/platform-api-service typecheck`：退出 0；`pnpm build`：退出 0。
- `pnpm verify:stage -- --stage V2.1 --scenario normal --seed 20260925`：退出 0，3 条断言 PASS。
- `pnpm verify:stage -- --stage V2.2 --scenario recovery --seed 20260925`：退出 0，分钟导入幂等断言 PASS。
- 真实 API 创建并读取：
  - V2.1 `testRunId=a96ffd32-9480-4f0c-9a66-159b306d6cbe`，normal，3 条断言均 PASS。
  - V2.2 `testRunId=5b8fe90d-c8f8-448e-b016-42228fd98598`，recovery，分钟导入第二次返回 `idempotent=true`，断言 PASS。
- 重启 `platform-api-service` 后，以上两个 runId 均按 owner 重新读取成功，状态、断言和证据保持一致。
- 使用 `another-user` 查询 V2.1 run 返回 HTTP 403；未泄漏运行记录。

## 状态与边界

P3.6 工程实现、数据库持久化、重启恢复和越权验证完成。V2.1/V2.2 Web 同 run 页面和用户人工签署仍未据此自动标记通过；人工结论保持 `NOT_RUN`。
