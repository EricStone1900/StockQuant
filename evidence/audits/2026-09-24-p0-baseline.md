# P0 基线与 P1 隔离验证

日期：2026-09-24（Asia/Shanghai）。执行基线：`132f3a6055b7a2cf124dd489ca71d0983c64698d`。

## P0 只读基线

- Git 工作区在执行前只有计划和审查记录未跟踪；没有修改既有业务文件。
- Compose 运行13个容器；配置健康检查的服务均为 `healthy`。
- `pnpm v24:preflight`：退出0，平台/行情/调度/日历/实时行情5项 PASS。
- `pnpm dc08a:supervise -- --check-only`：退出0，`HEALTHY`。
- `pnpm dc08a:verify-automation`：退出0，正式自动化无失败；20只订阅守卫和日终任务处于预期状态。
- 正式Paper观察：`WAITING`，8/20，剩余12日。
- 正式采集订阅：`dc08a-20260917-20-v1`，20只证券，采集执行器 `ENABLED`。

## P1 隔离改动与验证

创建专用数据库 `market_data_test` 和角色 `market_data_test`。`scripts/test-market-data-integration.mjs` 现在默认连接该数据库，并拒绝数据库名为 `market_data` 或用户为 `market_data` 的连接串。

- `pnpm test:integration:market-data`：退出0，17/17 PostgreSQL 集成测试通过。
- 使用正式库连接串的保护测试：退出2，输出明确拒绝原因。
- 测试库迁移后公共表数量为11。
- 正式库活动订阅数量仍为1。
- P1后只读复核：Paper仍为8/20 WAITING；正式20只集合和采集执行器保持不变；调度器仍为 `ENABLED`。

P1完成条件已满足：集成测试不再默认触碰正式采集库；正式订阅、调度和观察计数在测试前后保持可重读且未改变。
