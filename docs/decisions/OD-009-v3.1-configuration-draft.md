# OD-009：V3.1 研究配置决策草案

状态：`DRAFT`；source commit、Provider/模型和预算已冻结，凭证、外发和 Runner 兼容性仍未完成；不改变 `docs/decisions/open-decisions.md` 的整体未决状态。

日期：2026-09-21（Asia/Shanghai）

本草案用于与 V3.1 后端、Web、Runner 和验收证据并行准备。未获得项目所有者确认、真实模型凭证和实际镜像/运行证据前，不启用真实模型，不启动真实 RD-Agent，不宣称 V3.1 闭环通过。

## 1. 已可冻结的安全基线

| 配置项 | 草案值 | 依据/限制 |
|---|---|---|
| 研究环境 | `RESEARCH` | V3.1 契约已固定 |
| 交易通道 | `FAKE` | 三版共同规则；禁止真实券商 |
| 默认轮数 | `1` | `fixtures/v3/v3.1/manifest.json` 与现有准备场景 |
| 最大轮数 | `3` | V3.1 实验契约已固定 |
| 预算币种 | `USD` | 预算以美元分记录，避免不同 Provider 单位混淆 |
| 单轮默认预算 | `300` cents（$3） | V3.1 默认一轮准备值 |
| 单实验预算上限 | `1000` cents（$10） | 硬上限；超过即拒绝新实验 |
| V3.1 阶段总预算 | `3000` cents（$30） | 阶段门槛；预算账本完成前由验收门禁核对 |
| 预算告警/停止 | `80%` / `100%` | 80% 告警，100% 停止新调用；耗尽后不自动重试 |
| Worker 并发 | `1` | V3 计划的小样本基线 |
| Runner 状态 | `NOT_CONFIGURED` | 当前 Compose 已使用该安全值 |
| Model Gateway 状态 | `NOT_CONFIGURED` | 当前 Compose 已使用该安全值 |
| 无凭证/无 Runner 行为 | `PENDING_PREREQUISITES` | 现有服务行为；不得静默降级为录制响应并宣称真实通过 |
| 外发策略 | 默认拒绝 | 真实 Provider、域名、端口和限额确认前不开放任意外发 |

## 2. 仍需项目所有者确认的决策字段

以下字段保持 `UNSET`，不能由开发者擅自填写：

| 字段 | 当前值 | 接受条件 |
|---|---|---|
| RD-Agent source commit | `v0.8.0` / `274e274d5dbb72cc2ea139d1a7c93d73ce9b1198`（已冻结） | 官方仓库当前最高稳定发布标签；记录见 `evidence/local/V3.1/rd-agent-source-v0.8.0.json` |
| 模型 Provider | `DeepSeek`（已确认） | Chat 模型 Provider；真实调用仍需网关和凭证门禁 |
| 模型标识 | `deepseek-flash`（已确认） | LiteLLM 配置名为 `deepseek/deepseek-flash`；真实调用仍未启用 |
| Embedding Provider | `SiliconFlow`（已确认） | 使用独立 embedding 端点 |
| Embedding 模型 | `Qwen/Qwen3-Embedding-4B`（已确认） | LiteLLM 配置名为 `litellm_proxy/Qwen/Qwen3-Embedding-4B`；V3.1 输出维度固定 `1024` |
| 凭证引用名 | `DEEPSEEK_API_KEY`、`SILICONFLOW_API_KEY` | 阶段七只核验本机 `.env.local` 两项非空且权限为 `0600`；没有读取或记录值，尚未用于 Provider 请求；只记录 Secret/环境引用名，不写入 Git、镜像或日志 |
| 真实模型预算 | `USD 300/1000/3000 cents`（已确认） | 单轮默认/单实验硬上限/阶段总预算；未知计费请求标记 `UNKNOWN`，不自动重试 |
| 外发 allowlist | `UNSET` | 只允许模型网关所需目标；默认拒绝其他网络访问 |
| RD-Agent/Runner 镜像 digest | `UNSET` | Linux 目标环境构建并记录 immutable digest |
| Runner CPU/内存/时限/进程限制 | `UNSET` | 目标容器实测后冻结；超过限制必须隔离并保留失败证据 |

## 3. 可并行执行的准备工作

1. 在现有 `research-automation-service` 上冻结配置 Schema、错误码和 `PENDING_PREREQUISITES` 门禁，不接入真实 Provider。
2. 补齐控制器/Runner 协议草案：镜像白名单、只读输入、专属输出、资源限制、网络策略和 Artifact 引用。
3. 为 `normal`、`rejection`、`recovery` 保持同一 TestRun API，继续验证幂等、取消、失败留痕和 check-only。
4. 准备 Linux 容器构建脚本和依赖锁收集清单；在 source commit、Provider 和 digest 未确认前只做构建准备，不启动真实实验。
5. 准备配置校验报告，明确 `UNSET` 字段、阻塞原因和安全行为；不修改 V3.1 验收表的人工签署或真实闭环结论。

## 4. OD-009 接受前的门禁

- source commit、Provider、模型和预算已确认并留存记录；凭证引用、外发 allowlist 和 Runner 兼容性仍需完成。
- RD-Agent、Qlib 和 Runner 镜像在目标 Linux 架构构建成功，记录依赖锁、digest 和资源限制。
- Runner 拒绝 Docker Socket、秘密、业务数据库、其他 run 和未授权网络；越权/超时/OOM 有实际证据。
- 真实模型→生成代码→Qlib 评价至少完成一次，保存请求/响应摘要、代码、输入、错误、费用和 Artifact。
- 更新 OD-009 状态、V3.1 阶段文件、验收表和准备证据；在此之前继续保持 `NOT_RUN`。

### 已冻结的 RD-Agent 来源

本次选择官方仓库 `microsoft/RD-Agent` 的 `v0.8.0`，完整 commit 为 `274e274d5dbb72cc2ea139d1a7c93d73ce9b1198`。该版本要求 Python `>=3.10`；其 Qlib Dockerfile 固定 Qlib commit `3e72593b8c985f01979bebcf646658002ac43b00`，并使用 CUDA 基础镜像。RD-Agent commit 已冻结，但 Linux ARM64/Qlib 镜像构建仍未验证，因此 OD-009 整体状态保持 `OPEN`。
